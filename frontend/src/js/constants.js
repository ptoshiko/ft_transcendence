export const HOSTNAME = "localhost"
export const BACKEND_PORT = "8081"
export const API_ADDRESS = `https://${HOSTNAME}:${BACKEND_PORT}/api`;
export const AVATAR_ADDRESS = `https://${HOSTNAME}:${BACKEND_PORT}`;